
HEAD_PROB_FAIR = 0.5        # probability of flipping a head when the coin is fair
HEAD_PROB_UNFAIR = 0.45     # probability of flipping a head when the coin is unfair

# TIME_S   = 100    # simulation length (how many years we want to simulate the patient for)
ALPHA = 0.05        # significance level

# settings for steady-state simulation
SIM_SET_SIZE = 1000     # number of games of the simulated game set (each cohort would have 1000 games)

# settings for transient-state simulation
REAL_SET_SIZE = 10      # size of the real number of games to make the projections for
NUM_SIM_SETS = 1000  # number of simulated game sets used for making projections
